extends layout

block content
  h1  #{company.title}
  
  p #[strong Person:] 
    a(href=company.person.url) #{company.person.name}
  //p #[strong Summary:] #{company.summary}
  //p #[strong ISBN:] #{company.isbn}
  p #[strong Firm:] 
    each val in company.firm
      a(href=val.url) #{val.name}
      |, 
  
  div(style='margin-left:20px;margin-top:20px')
    //h4 Copies

    // each val in company_instances
    //   hr
    //   if val.status=='Available'
    //     p.text-success #{val.status}
    //   else if val.status=='Maintenance'
    //     p.text-danger #{val.status}
    //   else
    //     p.text-warning #{val.status}
    //   p #[strong Imprint:] #{val.imprint}
    //   if val.status!='Available'
    //     p #[strong Due back:] #{val.due_back}
    //   p #[strong Id:]
    //     a(href=val.url) #{val._id}
    //
    // else
    //   p There are no copies of this company in the library.
      
  hr
  p
    a(href=company.url+'/delete') Delete Company
  p
    a(href=company.url+'/update') Update Company